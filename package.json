{
  "name": "tos-tq-exam-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "dev": "npm run build && npm start",
    "test": "npm --prefix backend test"
  },
  "engines": { "node": ">=22.5" }
}
